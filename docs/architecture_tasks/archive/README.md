# Archived Architecture Documents

此目录用于保留 v1.0、v1.1、未修正路径歧义的 v1.2，以及 canonical 文件被替换前的历史副本。

- 历史文件不得用于生成新的 phase contract；
- 不删除、不静默覆盖、不改写历史正文；
- 文件头应标记 `DEPRECATED` 或 `SUPERSEDED`，或通过文件名明确历史版本；
- 当前基准为上级目录 canonical v1.2.1。
